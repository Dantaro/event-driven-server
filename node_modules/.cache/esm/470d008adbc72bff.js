let express,uuidv4,UserEventDriver,MathService;_dbc‍.w("express",[["default",["express"],function(v){express=v}]]);_dbc‍.w("uuid",[["default",["uuidv4"],function(v){uuidv4=v}]]);_dbc‍.w("./usereventdriver/UserEventDriver",[["default",["UserEventDriver"],function(v){UserEventDriver=v}]]);_dbc‍.w("./math/MathService",[["default",["MathService"],function(v){MathService=v}]]);// ESM syntax is supported.





const app = express()
app.use(express.static('public'))

app.get('/register', (req, res) => {
    res.writeHead(200, {
        'Content-Type': 'text/event-stream',
        'Cache-Control': 'no-cache',
        'Connection': 'keep-alive'
    })
    const userId = uuidv4()
    UserEventDriver.addUser(userId, res)
    UserEventDriver.fireEvent(userId, uuidv4(), { userId })
})

app.get('/test', (req, res) => {
    const eventId = uuidv4()
    UserEventDriver.fireEvent(req.query.userId, eventId, {test:'hello'})
    res.write(eventId)
    res.end()
})

app.get('/math/add', (req, res) => {
    const eventId = uuidv4()
    MathService.add(
        {
            eventId,
            userId: req.query.userId
        }, 
        req.query.number1, 
        req.query.number2
    )
    res.write(eventId)
    res.end()
})

app.listen(3000, () => console.log('SSE app listening on port 3000!'))

