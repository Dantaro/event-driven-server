_976‍.x([["default",()=>_976‍.o]]);const UserEventDriver = (function(obj) {
    const users = {}
    obj.addUser = (userId, res) => {
        users[userId] = (eventId, data) => {
            res.write(`id: ${eventId}\n`)
            res.write(`data: ${JSON.stringify(data)}\n\n`) 
        }
        return () => { 
            res.end()
            delete users[userId] 
        }
    }

    obj.fireEvent = (userId, eventId, data) => {
        if (users[userId]) {
            setTimeout(() => users[userId](eventId, data))
        }
    }

    return obj
})({})

_976‍.d(UserEventDriver);